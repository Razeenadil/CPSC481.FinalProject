<Window x:Class="CPSC481.FinalProject.RoutineStartScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CPSC481.FinalProject"
        mc:Ignorable="d"
        Title="RoutineStartScreen" Height="800" Width="450">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="550"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.ColumnSpan="2" Background="Gainsboro" BorderBrush="DarkSlateGray" BorderThickness="0,0,0,3"/>
        <TextBlock Margin="10,0,0,0" Grid.Column="1" VerticalAlignment="Center" FontSize="30" 
                   FontWeight="Bold" TextTrimming="CharacterEllipsis">Routine 1</TextBlock>
        <Button Margin="0" Background="Transparent" BorderBrush="DarkSlateGray" BorderThickness="0,0,3,0">
            <TextBlock FontSize="16" FontWeight="Bold">Back</TextBlock>
        </Button>

        <StackPanel Grid.ColumnSpan="2" Grid.Row="1" Background="Gainsboro">
            <ItemsControl Name="icRoutineList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="75"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Ellipse Margin="20" />
                            <TextBlock Margin="20" FontSize="20" FontWeight="Bold"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"
                                       Text="{Binding Num}"/>
                            <TextBlock Margin="20" FontSize="20" FontWeight="Bold" 
                                       Grid.Column="1" Text="{Binding Name}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <Border Grid.Row="2" Grid.ColumnSpan="2" Background="Gainsboro" BorderBrush="DarkSlateGray" BorderThickness="0,3,0,0"/>
        <Button Margin="37.5" Grid.Row="2" Grid.ColumnSpan="2" Height="75" Width="75" BorderThickness="1"
                HorizontalAlignment="Left" VerticalAlignment="Center" Background="Transparent">
            <Image Source="/Images/Pencil.png"/>
        </Button>
    </Grid>
</Window>
